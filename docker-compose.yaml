services:
  server:
    container_name: server
    build:
      context: ./src-server
      dockerfile: dockerfile
    ports:
      - 8081:3000

  client:
    container_name: client
    build:
      context: ./src-client
      dockerfile: dockerfile
      args:
        API_URL: http://localhost:8081/api
    ports:
      - 8080:80
